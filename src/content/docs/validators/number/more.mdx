---
title: 'more'
description: 'This validator checks if a number is more than a given number.'
---

## Import

```js
import { more } from '@prismane/core/validators';
```

## Parameters

The `more` validator accepts the following parameters:

| Name        | Type                  | Description                       |
| ----------- | --------------------- | --------------------------------- |
| `value`     | _number_              | The value that will be validated. |
| `number`    | _number_              | The number that will be compared. |
| `fieldName` | _string_, _undefined_ | The name of the field.            |

## Usage

The `more` validator tests the provided value and checks if it is more the provided number.

```ts
const value = 10;

const number = 20;

const error = more(value, number);

console.log(error); // This number must be more than 20!
```

If there is no error, _null_ is returned.

```ts
const value = 30;

const number = 20;

const error = more(value, number);

console.log(error); // null
```

### Custom Error Message

Pass the _fieldName_ parameter to the validator, which is the third parameter, to customize the error message to make it more personalized to the given field.

```ts
const value = 10;

const number = 20;

const error = more(value, number, 'Age');

console.log(error); // Age must be more than 20!
```

### useForm Example

The `more` validator, like all other validators of Prismane, has a great integration with our [`useForm`](/docs/hooks/use-form) hook.

Firstly, import the `useForm` hook like so:

```js
import { useForm } from '@prismane/core/hooks';
```

Then do the following:

```jsx preview
function Demo() {
  const { handleSubmit, register } = useForm({
    fields: {
      number: {
        value: 0,
        validators: {
          more: (v: string) => more(v, 5),
        },
      },
    },
  });

  return (
    <Form
      onSubmit={(e: any) => {
        handleSubmit(e, (v: any) => console.log(v, ""));
      }}
    >
      <NumberField
        placeholder="Enter your number:"
        label="Number:"
        {...register("number")}
      />
      <Button type="submit">Submit</Button>
    </Form>
  );
}
```
