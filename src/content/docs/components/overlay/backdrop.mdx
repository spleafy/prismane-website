---
title: 'Backdrop'
description: "Backdrop displays a container that is used to limit the user's actions and make them focus on it's content."
versatile: false
---

## Import

```js
import { Backdrop } from '@prismane/core';
```

## Usage

```jsx preview
function Demo() {
  const [shown, setShown] = useState(false);

  return (
    <>
      <Button onClick={() => setShown(true)}>Show Backdrop</Button>
      {shown && (
        <Backdrop onClick={() => setShown(false)}>
          <Text>This is the backdrop! Click on the backdrop to close it!</Text>
        </Backdrop>
      )}
    </>
  );
}
```

### Remove blur effect

You can remove the blur effect by using the _bft_ prop, which will will overwrite the _backdrop-filter_ property.

```jsx preview
function Demo() {
  const [shown, setShown] = useState(false);

  return (
    <>
      <Button onClick={() => setShown(true)}>Show Backdrop</Button>
      {shown && (
        <Backdrop onClick={() => setShown(false)} bft="blur(0px)">
          <Text>
            This backdrop does not have blur! Click on the backdrop to close it!
          </Text>
        </Backdrop>
      )}
    </>
  );
}
```

### Change the background color

To change the background color, you can use the _bg_ prop.

```jsx preview
function Demo() {
  const [shown, setShown] = useState(false);

  return (
    <>
      <Button onClick={() => setShown(true)}>Show Backdrop</Button>
      {shown && (
        <Backdrop onClick={() => setShown(false)} bg={['diamond', 500, 0.1]}>
          <Text>
            This backdrop has a different color! Click on the backdrop to close
            it!
          </Text>
        </Backdrop>
      )}
    </>
  );
}
```

## API

Please refer to the documentation below for a comprehensive overview of all the available props and classes for the mentioned components.

- [`Backdrop`](/docs/components-api/backdrop-api)
