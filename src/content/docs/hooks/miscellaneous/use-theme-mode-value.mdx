---
title: 'useThemeModeValue'
description: 'useThemeModeValue hook simplifies the logic for using two style properties for dark and light modes.'
---
## Overview
The useThemeModeValue hook streamlines the process of applying different values based on the current theme mode (dark or light). It accepts two arguments, one for the dark theme and another for the light theme. The hook then returns the appropriate value based on the active theme, simplifying the implementation of theme-aware styling in your components.

## Import

```js
import { useThemeModeValue } from '@prismane/core';
```

The `useThemeModeValue` hook accepts the following parameters:

| Name    | Type  | Description                                | Default Value |
| ------- | ----- | ------------------------------------------ | ------------- |
| `dark`  | _any_ | The respective value for dark theme mode.  | -             |
| `light` | _any_ | The respective value for light theme mode. | -             |

## Return Value

The `useThemeModeValue` hook returns the following variable:

| Name    | Type  | Description               |
| ------- | ----- | ------------------------- |
| `value` | _any_ | The current active value. |

## Usage

```jsx preview
function Demo() {
  const { toggleThemeMode } = usePrismaneTheme();

  const background = useThemeModeValue(['base', 300], ['base', 900]);

  return (
    <Center w="100%" h={fr(72)} bg={background}>
      <Button onClick={toggleThemeMode}>Toggle Theme Mode</Button>
    </Center>
  );
}
```
