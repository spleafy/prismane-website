## Import

```js
import {
  BreadcrumbProps,
  BreadcrumbItemProps,
  BreadcrumbSeparatorProps,
} from "@prismane/core";
```

## Props

### Breadcrumb

`Breadcrumb` extends [Flex](/docs/components-api/flex-api) component's props.

### Breadcrumb.Item

`Breadcrumb.Item` extends [Flex](/docs/components-api/flex-api) component's props.

### Breadcrumb.Separator

`Breadcrumb.Item` extends [Flex](/docs/components-api/flex-api) component's props.

## CSS

### Breadcrumb

`Breadcrumb` extends [Flex](/docs/components-api/flex-api) component's CSS classes.

| Rule   | Global Class               | Description                       |
| ------ | -------------------------- | --------------------------------- |
| `root` | `.PrismaneBreadcrumb-root` | The wrapper box of the container. |

### Breadcrumb.Item

`Breadcrumb.Item` extends [Flex](/docs/components-api/flex-api) component's CSS classes.

| Rule   | Global Class                   | Description                       |
| ------ | ------------------------------ | --------------------------------- |
| `root` | `.PrismaneBreadcrumbItem-root` | The wrapper box of the container. |

### Breadcrumb.Separator

`Breadcrumb.Separator` extends [Flex](/docs/components-api/flex-api) component's CSS classes.

| Rule   | Global Class                        | Description                       |
| ------ | ----------------------------------- | --------------------------------- |
| `root` | `.PrismaneBreadcrumbSeparator-root` | The wrapper box of the container. |
