## Import

```js
import {
  TableProps,
  TableBodyProps,
  TableCaptionProps,
  TableCellProps,
  TableFootProps,
  TableHeadProps,
  TableRowProps,
} from "@prismane/core";
```

## Props

### Table

`Table` extends [Box](/docs/components-api/box-api) component's props.

### Table.Row

`Table.Row` extends [Box](/docs/components-api/box-api) component's props.

### Table.Cell

`Table.Cell` extends [Box](/docs/components-api/box-api) component's props.

### Table.Head

`Table.Head` extends [Box](/docs/components-api/box-api) component's props.

### Table.Foot

`Table.Foot` extends [Box](/docs/components-api/box-api) component's props.

### Table.Body

`Table.Body` extends [Box](/docs/components-api/box-api) component's props.

### Table.Caption

`Table.Caption` extends [Box](/docs/components-api/box-api) component's props.

| Name        | Type           | Description                                 | Default |
| ----------- | -------------- | ------------------------------------------- | ------- |
| `placement` | `bottom / top` | The placement of the table caption element. | bottom  |

## CSS

### Table

`Table` extends [Box](/docs/components-api/box-api) component's CSS classes.

| Rule   | Global Class          | Description                           |
| ------ | --------------------- | ------------------------------------- |
| `root` | `.PrismaneTable-root` | The container of the table component. |

### Table.Row

`Table.Row` extends [Box](/docs/components-api/box-api) component's CSS classes.

| Rule   | Global Class             | Description                               |
| ------ | ------------------------ | ----------------------------------------- |
| `root` | `.PrismaneTableRow-root` | The container of the table row component. |

### Table.Cell

`Table.Cell` extends [Box](/docs/components-api/box-api) component's CSS classes.

| Rule   | Global Class              | Description                                |
| ------ | ------------------------- | ------------------------------------------ |
| `root` | `.PrismaneTableCell-root` | The container of the table cell component. |

### Table.Head

`Table.Head` extends [Box](/docs/components-api/box-api) component's CSS classes.

| Rule   | Global Class              | Description                                |
| ------ | ------------------------- | ------------------------------------------ |
| `root` | `.PrismaneTableHead-root` | The container of the table head component. |

### Table.Foot

`Table.Foot` extends [Box](/docs/components-api/box-api) component's CSS classes.

| Rule   | Global Class              | Description                                |
| ------ | ------------------------- | ------------------------------------------ |
| `root` | `.PrismaneTableFoot-root` | The container of the table foot component. |

### Table.Body

`Table.Body` extends [Box](/docs/components-api/box-api) component's CSS classes.

| Rule   | Global Class              | Description                                |
| ------ | ------------------------- | ------------------------------------------ |
| `root` | `.PrismaneTableBody-root` | The container of the table body component. |

### Table.Caption

`Table.Caption` extends [Box](/docs/components-api/box-api) component's CSS classes.

| Rule   | Global Class                 | Description                                   |
| ------ | ---------------------------- | --------------------------------------------- |
| `root` | `.PrismaneTableCaption-root` | The container of the table caption component. |
