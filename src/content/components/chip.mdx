# Chip

Chip displays a container that is used to highlight some important short information.

## Import

```js
import { Chip } from "@prismane/core";
```

## Usage

By default, `Chip` uses the `primary` color.

```jsx
<Chip>Default</Chip>
```

### Chip With Icon

```jsx
<Chip icon={<Fire />}>New Release</Chip>
```

### Chip Colors

By default, `Chip` uses the `primary` color.

```jsx
<>
  <Chip>Primary</Chip>
  <Chip color="ruby">Ruby</Chip>
  <Chip color="lime">Lime</Chip>
  <Chip color="teal">Teal</Chip>
</>
```

### Chip Sizes

```jsx
<>
  <Chip size="xs">Primary</Chip>
  <Chip size="sm">Primary</Chip>
  <Chip size="base">Primary</Chip>
  <Chip size="md">Primary</Chip>
  <Chip size="lg">Primary</Chip>
</>
```

## API

Please refer to the documentation below for a comprehensive overview of all the available props and classes for the mentioned components.

- [`Chip`](/docs/components-api/chip-api)
