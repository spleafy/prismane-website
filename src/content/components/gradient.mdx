<Versatile>Gradient</Versatile>

Gradient displays a container with gradient background.

## Import

```js
import { Divider } from "@prismane/core";
```

## Usage

By default, `Gradient` has the values of `from` - ["primary", 500], `to` - ["primary", 300], `deg` - 90

```jsx
<Gradient w={360} h={120} />
```

### Custom Gradient

By changing the values of the `from`, `to` and `deg` props, you can display a variety of different gradients. Hex colors are also supported.

```jsx
<Gradient w={360} h={120} from={["teal", 600]} to={["ruby", 600]} deg={45} />
```

### Versatile Component

```jsx
<Gradient
  as="a"
  href="https://www.google.com"
  target="_blank"
  w={360}
  h={120}
  from="copper"
  to="pink"
  deg={135}
/>
```

## API

Please refer to the documentation below for a comprehensive overview of all the available props and classes for the mentioned components.

- [`Gradient`](/docs/components-api/gradient-api)
